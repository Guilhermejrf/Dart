import 'package:flutter/material.dart';

class TelaAuditoria extends StatelessWidget {
  const TelaAuditoria({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('teste'),
      ),
    );
  }
}
