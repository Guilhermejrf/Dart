import 'package:flutter/material.dart';

class TelaMenuNav extends StatelessWidget {
  const TelaMenuNav({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('teste'),
      ),
    );
  }
}
